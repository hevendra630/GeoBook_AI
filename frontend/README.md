# Frontend (React)

## Setup

```bash
npm install
copy .env.example .env
npm run dev
```

## Configuration

- `VITE_API_BASE_URL`: FastAPI base URL (default `http://localhost:8000`)
- `VITE_GOOGLE_MAPS_JS_API_KEY`: optional (this starter uses an embed preview instead of JS API)

